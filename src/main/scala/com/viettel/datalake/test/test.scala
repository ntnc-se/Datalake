package com.viettel.datalake.test

import org.apache.spark.sql.types.{StringType, StructType}
import org.apache.spark.sql.{Row, SparkSession}

import java.util


object test {
//  this.appName = "test1"
//
//  def main(args: Array[String]): Unit = {
//    val a = "haha"
//    val row = Row.fromSeq(Seq(a))
//    val ss: SparkSession = SparkSession.builder().master("local")getOrCreate()
//    val list = new util.ArrayList[Row]()
//    list.add(row)
//    val df = ss.createDataFrame(list, new StructType().add("1", StringType))
//    df.show()
//  }
}
